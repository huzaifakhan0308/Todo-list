import _ from 'lodash';
import './style.css';

class NewList {
    constructor(NewValue, index) {
        this.completed = true
        this.inputValue = NewValue
        this.indexNumber = index
    }
}

const Array = []